[project]
name = "mini-rl-survival"
version = "0.1.0"
description = "Mini tabular RL survival gridworld + modular pygame viewer"
requires-python = ">=3.10"
dependencies = ["pygame>=2.5", "numpy>=1.24", "matplotlib>=3.7"]

[tool.pytest.ini_options]
testpaths = ["tests"]
