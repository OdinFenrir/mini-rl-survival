def pretty_print_grid(grid, agent_pos, food_pos, hazard_pos, cell_width=3):\n    import os\n    import colorama\n    from colorama import Fore, Back, Style\n    colorama.init()  # Initialize colorama\n    \n    # Define symbols and colors\n    symbols = {\n        'agent': '🤖',  # Agent emoji\n        'food': '🍎',   # Food emoji\n        'hazard': '💣', # Hazard emoji\n    }\n    colors = {\n        'agent': (Fore.WHITE, Back.GREEN),\n        'food': (Fore.BLACK, Back.RED),\n        'hazard': (Fore.BLACK, Back.YELLOW),\n        'empty': (Fore.BLACK, Back.WHITE),\n    }\n    \n    # Print header\n    print('┌' + ('─' * cell_width) * len(grid[0]) + '┐')\n    for r_index, row in enumerate(grid):\n        row_str = '│'  # Start the row with a left border\n        for c_index, cell in enumerate(row):\n            if (r_index, c_index) == agent_pos:\n                symbol = symbols['agent']\n                color = colors['agent']\n            elif (r_index, c_index) == food_pos:\n                symbol = symbols['food']\n                color = colors['food']\n            elif (r_index, c_index) in hazard_pos:\n                symbol = symbols['hazard']\n                color = colors['hazard']\n            else:\n                symbol = ' '  # Empty cell\n                color = colors['empty']\n            row_str += f'{color[1]}{symbol.center(cell_width)}{Style.RESET_ALL}'  # Add colored symbol\n        row_str += '│'  # End the row with a right border\n        print(row_str)\n        print('├' + ('─' * cell_width) * len(row) + '┤')  # Row separator\n    print('└' + ('─' * cell_width) * len(grid[0]) + '┘')\n    \n    # Print legend\n    print('\nLegend:')\n    for label, (symbol, color) in symbols.items():\n        print(f'{colorama.Fore.WHITE}{colorama.Back.BLACK}{symbol} : {label.capitalize()}{colorama.Style.RESET_ALL}')\n